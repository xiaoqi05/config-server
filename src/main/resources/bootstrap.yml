spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/spring-cloud-samples/config-repo
          username: trolley
          password: strongpassword
  application:
    name: config-server
  profiles:
    active: native
server:
  port: 7001

